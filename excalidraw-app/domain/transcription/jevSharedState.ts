/** Same evidence fields every Jev scale reads when context is on. */
export const JEV_SHARED_EVIDENCE =
  "`speaker_recent` is this speaker's own earlier lines. `recent_turns` and `previous_text` are the shared conversation; use them to resolve fragments and replies, not as extra speech to score. Do not inherit another speaker's feelings or attributes.";
